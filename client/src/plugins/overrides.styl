$body-font-family = 'Lato' !important
$heading-font-family = 'Lato' !important
$primary = #719125
$secondary = #f89c73
$lightaccent = #e2dbc9
$darkaccent = #484848
$accent = #8c9eff
$error = #b71c1c
$info = #2196F3
$success = #4CAF50
$warning = #FFC107
$light = #FAF8F0

.bm-aligned {
	text-align: center;
}

.bm-section-header {
	text-align: center;
	font-size: 1.8em;
	color: $darkaccent;
}

.bm-title {
	font-weight: 300;
	font-size: 2em;
	text-align: center;

	&.smaller {
		font-size: 1.2em;
	}
}

.bm-subtitle {
	text-transform: uppercase;
	letter-spacing: 0.06em;
	font-size: 0.8em;
	font-weight: 400;
}

.bm-title-card {
	font-size: 1.2em;
	color: $darkaccent;
	text-align: center;
}

.bm-text-card {
	color: $darkaccent;
	text-align: center;
}

.shadowed {
	text-shadow: 2px 2px 3px hsla(0, 0.9, 0.8, 0.4);
}

.image-filter {
	position: absolute;
	opacity: 0.5;
	width: 100%;
	height: 100%;
	top: 0px;

	&.darker {
		opacity: 0.25;
	}
}

.slide-left-enter-active, .slide-left-leave-active, .slide-right-enter-active, .slide-right-leave-active {
	transition-duration: 0.3s;
	transition-property: height, opacity, transform;
	transition-timing-function: cubic-bezier(0.55, 0, 0.1, 1);
	overflow: hidden;
}

.slide-left-enter, .slide-right-leave-active {
	opacity: 0;
	transform: translate(2em, 0);
}

.slide-left-leave-active, .slide-right-enter {
	opacity: 0;
	transform: translate(-2em, 0);
}
.love-ya {
	font-family: 'Love Ya Like A Sister';
}
@import '~vuetify/src/stylus/main'
